export enum StatusCodes {
    OK = 200,
    BAD_REQUEST = 400,
    NOT_FOUND = 401,
    FORBIDDEN = 403
}

export enum StatusMessages {
    CREATE = "Successfully created the user",
    UPDATE = "Successfully updated the user",
    DELETE = "Successfully deleted the user",
    ERROR = "Error encountered while "
}